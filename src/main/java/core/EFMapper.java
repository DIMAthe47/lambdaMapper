package core;

import model.E;
import model.EParent;
import model.F;
import model.FParent;

public class EFMapper extends EParentFParentMapper {

//    @Override
//    public void map(E source, F target) {

//        internalMap(() -> source.getE1(), (s) -> target.setF1(s));
//        internalMap(() -> source.getE2(), (s) -> target.setF2(s));
//    }
}
